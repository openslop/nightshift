[Unit]
Description=Nightshift nightly batch

[Timer]
OnCalendar=*-*-* 05:12:00
AccuracySec=1m
# Persistent stays off on purpose. A night missed while the machine slept is dropped, not run later in the day.

[Install]
WantedBy=timers.target
